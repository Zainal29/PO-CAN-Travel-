<x-app-layout><x-slot name="header"><h2 class="font-semibold text-xl text-gray-800">Detail Rute</h2></x-slot><div class="py-6"><div class="max-w-4xl mx-auto bg-white p-6 shadow rounded-xl space-y-3"><h3 class="text-xl font-bold">{{ $route->origin_city }} → {{ $route->destination_city }}</h3><p>{{ $route->origin_terminal }} → {{ $route->destination_terminal }}</p><p>Bus: {{ $route->bus->bus_name }} · {{ $route->bus->plate_number }}</p><p>{{ $route->departure_date->format('d/m/Y') }} pukul {{ $route->departure_time }} · Tiba {{ $route->estimated_arrival_time }}</p><p>Rp {{ number_format($route->price, 0, ',', '.') }} · {{ $route->available_seats }} kursi · {{ ucfirst($route->status) }}</p><a class="text-indigo-600" href="{{ route('admin.routes.index') }}">Kembali</a></div></div></x-app-layout>
