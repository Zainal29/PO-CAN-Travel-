<x-app-layout>
    <x-slot name="header"><div class="flex justify-between"><h2 class="font-semibold text-xl text-gray-800">Manajemen Rute</h2><a href="{{ route('admin.routes.create') }}" class="rounded-lg bg-indigo-600 px-4 py-2 text-white">Tambah Rute</a></div></x-slot>
    <div class="py-6"><div class="max-w-7xl mx-auto px-6"><div class="overflow-x-auto rounded-xl bg-white shadow"><table class="w-full text-left"><thead class="bg-gray-50"><tr><th class="px-6 py-3">Perjalanan</th><th class="px-6 py-3">Bus</th><th class="px-6 py-3">Jadwal</th><th class="px-6 py-3">Kursi</th><th class="px-6 py-3">Aksi</th></tr></thead><tbody>@forelse($routes as $route)<tr class="border-t"><td class="px-6 py-4">{{ $route->origin_city }} → {{ $route->destination_city }}</td><td class="px-6 py-4">{{ $route->bus->bus_name }}</td><td class="px-6 py-4">{{ $route->departure_date->format('d/m/Y') }} {{ $route->departure_time }}</td><td class="px-6 py-4">{{ $route->available_seats }} · {{ $route->status }}</td><td class="px-6 py-4"><a class="text-indigo-600" href="{{ route('admin.routes.show', $route) }}">Detail</a> <a class="ml-2 text-indigo-600" href="{{ route('admin.routes.edit', $route) }}">Edit</a><form action="{{ route('admin.routes.destroy', $route) }}" method="POST" class="inline ml-2">@csrf @method('DELETE')<button class="text-red-600" onclick="return confirm('Hapus rute ini?')">Hapus</button></form></td></tr>@empty<tr><td colspan="5" class="px-6 py-8 text-center text-gray-500">Belum ada rute.</td></tr>@endforelse</tbody></table><div class="p-6">{{ $routes->links() }}</div></div></div></div>
</x-app-layout>
